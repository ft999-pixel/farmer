# 把網站放到網路上（永久網址）

ngrok 只要關掉視窗就失效，網址每次都變。要一個「隨時都在、網址固定」的網站，就要部署。

本指南用 **Render** 免費方案（不用信用卡，介面最單純）。
其他選擇：Zeabur（台灣團隊、介面中文）、Railway、Fly.io，流程大同小異。

---

## ⚠️ 先讀這段：免費方案的兩個限制

**限制一：檔案改動會消失。**

Render 每次重新部署或伺服器重啟，硬碟會**還原成 GitHub 上的樣子**。也就是說：

| 東西 | 會怎樣 |
|---|---|
| 在「資料維護」後台改的補助 | **消失**，變回 GitHub 上的版本 |
| 卡點統計 `blockers.jsonl` | **歸零** |
| 後台的備份與稽核紀錄 | **消失** |

所以線上版的後台只適合「試用看看」，**真正要保留的補助資料修改，必須改完 commit 進 GitHub**。
（要真正保存，得付費開 Persistent Disk，或改接資料庫——本版不做。）

**限制二：沒人用就會睡著。**

免費方案閒置 15 分鐘會停機，下一個人打開要等 **30～60 秒**才醒來。

> 🎯 **決審／評審展示前，一定要先自己打開網頁把它叫醒**，再把網址給評審。
> 不然評審點開會看到一片空白轉圈圈。真的要現場展示，用 ngrok 或本機更保險。

---

## 步驟一：部署前的安全檢查

1. **改掉後台密碼**。打開 `.env`，把 `ADMIN_PASSWORD` 從預設值改成別人猜不到的：

   ```
   ADMIN_PASSWORD=你自己想的密碼
   ```

2. **確認 `.env` 不會被上傳**。它已經寫在 `.gitignore` 裡，執行下面確認（應該沒有任何輸出）：

   ```
   git check-ignore .env
   ```

   ⚠️ `.env` 裡有你的 Anthropic 金鑰。一旦上傳到 GitHub 就等於公開，任何人都能拿去用、算你的錢。

---

## 步驟二：把程式放上 GitHub

在專案資料夾開 cmd，依序執行：

```
git init
git add .
git commit -m "農民補給站 初版"
```

然後到 github.com 建一個新的 repository（**選 Private**，除非你確定要公開），
照它畫面上給的指令貼上，通常是：

```
git remote add origin https://github.com/你的帳號/你的repo名.git
git branch -M main
git push -u origin main
```

---

## 步驟三：在 Render 部署

1. 到 render.com 註冊，用 GitHub 帳號登入
2. 點 **New +** → **Web Service**
3. 選你剛剛上傳的那個 repository
4. 設定填這些：

| 欄位 | 填什麼 |
|---|---|
| Language | `Python 3` |
| Build Command | `pip install -r requirements.txt` |
| Start Command | `python -m uvicorn aidstation.api:app --host 0.0.0.0 --port $PORT --app-dir src` |
| Instance Type | `Free` |

5. 往下找到 **Environment Variables**，把 `.env` 裡的內容一條一條加進去
   （**不是上傳 `.env` 檔**，是在 Render 網頁上填）：

   | Key | Value |
   |---|---|
   | `ADMIN_PASSWORD` | 你改好的密碼 |
   | `ADMIN_SECRET` | 隨便一串長亂碼 |
   | `ANTHROPIC_API_KEY` | 你的金鑰（不想給就留空，會自動降級成關鍵字模式） |

6. 按 **Create Web Service**，等 3～5 分鐘

完成後會給你一個像 `https://你取的名字.onrender.com` 的網址，**這個網址不會變**，可以直接貼進簡報。

記得後面加 `/app/`：`https://你的名字.onrender.com/app/`

---

## 之後要更新網站怎麼做

改完程式後：

```
git add .
git commit -m "說明你改了什麼"
git push
```

Render 會自動偵測到並重新部署，等幾分鐘就生效。**不用再進 Render 網頁操作。**

---

## 出問題時看哪裡

Render 專案頁面左邊有 **Logs**，部署失敗或網站掛掉的原因都會寫在那。

| 狀況 | 通常是 |
|---|---|
| 部署失敗，log 出現 `ModuleNotFoundError` | 有套件沒寫進 `requirements.txt` |
| 網頁 502 / Application failed to respond | Start Command 打錯，或忘了 `--host 0.0.0.0` |
| 網頁開得起來但公文翻譯怪怪的 | `ANTHROPIC_API_KEY` 沒設，正在跑關鍵字降級模式（正常現象） |
| 第一次打開超級慢 | 免費方案睡著了，屬正常，等一下就好 |
