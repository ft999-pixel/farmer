# 欄位名稱統一：評估與建議

**日期**：2026-09-05
**背景**：專案中同時存在兩套欄位命名。前端／舊引擎用 `crop`、`township`、`event`；
新的 `MatchingProfile` 用 `crops`、`location`、`disaster_situation`。
本文評估「統一」要動哪些檔、風險在哪，供團隊決定後再動手。

---

## ✅ 執行進度（2026-09-05 更新）

團隊決議：採用第五節的三步驟，災害專用欄位放進通用 `MatchingProfile`。

| 步驟 | 狀態 | 實際改動 |
|---|---|---|
| 1. 三個欄位進 MatchingProfile | ✅ 完成 | `schemas.py` 新增 `loss_rate`／`land_doc`／`policy_enrolled_2y`；提問文字與白話選項接上 `fields.json` |
| 2. 相容層改單向 | ✅ 完成 | 後端維持內部舊名，`matching.py` 入口轉換一次 |
| 3. 前端改用新名 | ✅ 完成 | `profile.html` 送出前移除 `crop`／`township`／`event`／`certification` |

驗證：只送新名與只送舊名的比對結果**完全相同**；109 個測試通過；
新增 `tests/test_disaster_fields.py` 釘住豁免條款與白話選項。

其他一併處理：示範公告期改為 **2026-09-05～09-11**（原 8/01–8/14 已過期，
會讓災害救助顯示「已截止」）。

---

## 一、結論先講

**目前不建議做全面重新命名。**

原因：**相容層已經存在且正在運作**。`matching.py` 的 `_FIELD_ALIASES` 已經把三個主要
欄位雙向對應，實測兩種寫法比對結果完全相同。全面改名是高風險、低報酬的工程。

建議改為**三個小步驟**（見第五節），成本低、風險可控，也能拿掉「兩套命名」的混亂。

---

## 二、現況：兩套命名並存，中間有橋

### 2.1 已對應的欄位（`matching.py:47`）

| 舊名（fields.json） | 新名（MatchingProfile） | 狀態 |
|---|---|---|
| `crop` | `crops` | ✅ 已對應，且自動處理單值→list |
| `township` | `location`（另接受 `region`） | ✅ 已對應 |
| `event` | `disaster_situation` | ✅ 已對應 |
| `land_doc` | （只有自己） | ⚠️ 新模型沒有對應欄位 |

### 2.2 只存在於舊模型、新模型沒有的欄位

| 欄位 | 用途 | 新模型有無 |
|---|---|---|
| `loss_rate` | 災損幾成，災害救助的核心判準 | ❌ 沒有 |
| `land_doc` | 土地文件種類，決定能否免附 | ❌ 沒有 |
| `policy_enrolled_2y` | 近兩年政策申報，**豁免條款的關鍵** | ❌ 沒有 |

> ⚠️ **這三個是災害救助流程的核心。** 如果統一時直接砍掉，
> 「口頭租約 ＋ 曾申報政策 → 土地文件免附」這條豁免規則會失效。
> 那是設計建議書列為關鍵價值的功能，也是決賽 Demo 劇本的橋段。

### 2.3 兩邊同名、無爭議

`age`、`land_tenure`、`is_farming`、`insured_farmer`

---

## 三、實測結果

```
輸入 crops/location/disaster_situation → 農業保險 MATCH、災害救助 NEED_INFO
輸入 crop/township/event               → 結果完全相同
```

**兩種寫法目前產生一樣的結果**，相容層有效。

---

## 四、若要全面改名，要動的檔案

依實際 grep 統計（不含 `data/` 的執行期產物如 blockers、備份、members.db）：

| 檔案 | 影響 | 風險 |
|---|---|---|
| `data/fields.json` | 欄位字典本體，含**台語別名表**（檨仔→芒果） | 🔴 高。別名表是打字輸入台語的唯一依據 |
| `data/programs/*.json` × 5 | 舊結構的 `eligibility` 條件樹直接引用欄位名 | 🔴 高。改錯 = 該筆補助永遠比對不到 |
| `src/aidstation/engine.py` | 三值邏輯引擎，45 處引用 | 🔴 高 |
| `src/aidstation/flow.py` | 對話狀態機，legacy 路徑 | 🔴 高 |
| `src/aidstation/matching.py` | 相容層本身 | 🟡 中。統一後這層才能拿掉 |
| `src/aidstation/extract.py` | LLM 受控抽取的欄位白名單 | 🟡 中 |
| `src/aidstation/fields.py` | 正規化（別名、型別轉換） | 🟡 中 |
| `web/profile.html`、`browse.html`、`site-chat.js` | 前端表單與顯示 | 🟡 中 |
| `tests/` × 約 8 檔 | 舊欄位名出現 60+ 次 | 🟢 低但量大 |

### 風險最高的三件事

1. **豁免條款失效**：`policy_enrolled_2y` 沒有新模型對應，砍掉會讓
   「曾申報政策 → 免附土地文件」失效。這是專案對實耕者最核心的價值。
2. **災害救助比對失準**：`loss_rate`（損失幾成）是法定判準（作業要點§6，兩成以上），
   新模型沒有這個概念。
3. **台語別名表遺失**：`fields.json` 的 `aliases` 若在重構中被略過，
   農民打「檨仔」將查不到芒果的補助。

---

## 五、建議做法：三個小步驟取代大重構

### 步驟 1：把缺的三個欄位補進 MatchingProfile（半天）

在 `schemas.py` 的 `MatchingProfile` 增加：

```python
loss_rate: float | None = None            # 災損成數，0.2 = 兩成
land_doc: str | None = None               # 土地文件種類
policy_enrolled_2y: bool | None = None    # 近兩年政策申報（豁免條款依據）
```

**這一步單獨做就有價值**：新引擎終於看得懂災害救助的判準，不必等統一。

### 步驟 2：把相容層變成單向（半天）

目前 `_FIELD_ALIASES` 是雙向接受。改成：

- **對外（API、前端）一律用新名**：`crops`、`location`、`disaster_situation`
- **對內（fields.json、eligibility 條件樹）維持舊名**，由 `normalize_facts` 在入口轉換一次

好處：外部只有一套名字，內部不用動 5 個補助的條件樹，風險大幅下降。

### 步驟 3：前端改用新名（半天）

`profile.html`、`browse.html`、`site-chat.js` 送出時改用 `crops`/`location`/`disaster_situation`。
因為相容層還在，這一步可以逐頁進行，改一頁測一頁，不必一次到位。

**總計約 1.5 天，且每一步都可獨立驗證、可隨時停下。**

---

## 六、不建議的做法

| 做法 | 為什麼不建議 |
|---|---|
| 全面改名 `fields.json` 與 5 個條件樹 | 高風險，且沒有對應的功能收益。條件樹是法規判準的載體，改錯不會報錯，只會安靜地比對不到 |
| 直接刪掉舊引擎 `engine.py` | 5 筆舊補助的三值邏輯（符合／可能符合／不符合）與法條歸因依賴它。新引擎的 `MATCH/NEED_INFO` 語意不同 |
| 保持現狀不處理 | 兩套命名會持續造成誤解，新人接手容易寫錯 |

---

## 七、需要團隊決定的事

1. **要不要保留舊引擎 `engine.py`？**
   它負責 5 筆舊補助的三值邏輯與法條歸因（每條結果附 `legal_ref`）。
   新引擎目前沒有等價的歸因輸出。

2. **`loss_rate` / `policy_enrolled_2y` 這類災害專用欄位，放進通用的
   `MatchingProfile`，還是另立一個 `DisasterProfile`？**
   前者簡單，後者乾淨。

3. **步驟 3 的前端改動要不要等 Demo 之後再做？**
   Demo 前動前端有風險，而且相容層在，不改也能運作。

---

## 八、附帶發現（與本主題無關但需處理）

- **`農業天然災害現金救助` 目前比對為 CLOSED**：示範公告期 2026-08-01～08-14 已過期。
  展示時請用 `?demo=1`（固定日期 2026-08-20），否則評審會看到「已截止」。
- **`module/prefill/`（Flask, port 5000）未被部署**：`render.yaml` 只啟動 uvicorn。
  若要讓 `/api/profiles` 在線上可用，需另外安排部署。
