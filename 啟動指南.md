# 在自己電腦上啟動農民補給站（Windows）

## 一次性安裝

1. 安裝 Python：到 python.org 下載 3.11 以上版本，安裝時**勾選「Add Python to PATH」**。
2. 開啟「命令提示字元」（按 Win 鍵打 cmd），執行：

```
cd C:\Users\futong\Claude\Projects\農民補助系統
pip install -r requirements.txt
```

## 每次啟動

```
cd C:\Users\futong\Claude\Projects\農民補助系統
python scripts\check_keys.py
python run.py
```

（`run.py` 會自動處理模組路徑，cmd 和 PowerShell 都能用，不需要設 PYTHONPATH）

看到 `Uvicorn running on http://127.0.0.1:8000` 就是活了。

| 開哪個網址 | 看到什麼 |
|---|---|
| http://127.0.0.1:8000/app/ | 網站首頁（農民端） |
| http://127.0.0.1:8000/app/dashboard.html | 卡點儀表板（機關端） |
| http://127.0.0.1:8000/app/admin.html | 補助資料維護後台（需密碼） |
| http://127.0.0.1:8000/docs | 所有 API，可直接測試 |

（連接埠被占用時：`set PORT=8001` 再 `python run.py`）

## 改了東西之後要怎麼套用

| 改了什麼 | 要做什麼 |
|---|---|
| `web\` 底下的 html／js／css | **重新整理瀏覽器就好**（Ctrl+F5 強制重新整理） |
| `data\programs\*.json` 補助資料 | 用後台改就即時生效；直接改檔案則要重啟 |
| `src\aidstation\*.py` 程式碼 | **必須重啟**：在跑伺服器的那個視窗按 `Ctrl+C`，再 `python run.py` |

⚠️ 改了 `.py` 卻沒重啟，是最容易被騙到的狀況：畫面不會報錯，只是跑的還是舊程式碼，
你會以為改的東西沒有效果。改完 `.py` 就重啟，養成習慣最省事。

想要存檔自動重啟（可選）：

```
pip install watchfiles
set RELOAD=1
python run.py
```

⚠️ 沒裝 `watchfiles` 就設 `RELOAD=1` 會更糟：uvicorn 會退回 StatReload，
在 Windows 上會顯示「detected changes, Reloading...」但舊程式繼續服務——
看起來有重啟，其實沒有。要嘛裝 watchfiles，要嘛就乖乖手動重啟。

## 讓隊友也看得到

### 情況一：隊友跟你在同一個 Wi-Fi（辦公室、教室）← 最簡單

```
set HOST=0.0.0.0
python run.py
```

啟動訊息會直接印出要貼給隊友的網址，像這樣：

```
隊友看這個：http://10.37.189.68:8000/app/   ← 把這行貼給隊友
```

隊友用手機或電腦開那個網址就看得到。**你的電腦不能關、不能休眠**，關掉隊友就連不上了。

第一次執行時 Windows 防火牆會跳出詢問，要按「**允許存取**」，否則隊友連不進來。

### 情況三：要固定網址、隨時都能開 → 部署上線

ngrok 關掉就失效、網址每次都變。要放進簡報的固定網址，看 [部署指南.md](部署指南.md)。

### 情況二：隊友在外地 → 用 ngrok

**要開兩個視窗，兩個都不能關。**

第一個視窗，先把網站跑起來：

```
python run.py
```

第二個視窗（另外開一個 cmd），再跑 ngrok：

```
ngrok http 8000
```

ngrok 會出現一張表，找 `Forwarding` 那一行：

```
Forwarding    https://1a2b-59-120-33-45.ngrok-free.app -> http://localhost:8000
              └────────── 複製這一段給隊友 ──────────┘
```

⚠️ **上面那串 `1a2b-59-120-33-45` 是舉例，你的一定不一樣。**
不要照抄本文件的網址，要看你自己 ngrok 視窗印出來的那一行。
每次重開 ngrok 網址都會變，要重新貼給隊友。

隊友開的網址要記得加 `/app/`，例如：
`https://你的網址.ngrok-free.app/app/`

**常見錯誤**

| 畫面顯示 | 原因 |
|---|---|
| `ERR_NGROK_3200 ... is offline` | ngrok 視窗沒開著，或網址打錯（例如照抄了文件裡的範例） |
| ngrok 有開但顯示 502 | 網站沒跑起來，或 `ngrok http` 後面的埠號跟 `python run.py` 的不一樣 |

### ⚠️ 對外開放前一定要做的兩件事

1. **改掉後台密碼**。`.env` 裡的 `ADMIN_PASSWORD` 目前是預設值 `change-me-please`，
   任何人只要開 `/app/admin.html` 猜到密碼，就能改你的補助資料。
2. **知道 API 金鑰會被用掉**。公文翻譯會呼叫 Anthropic API，別人狂用會算在你的帳上。
   給不熟的人看之前，可以先把 `.env` 的 `ANTHROPIC_API_KEY` 清空（會自動降級成關鍵字模式，不影響展示）。

ngrok 的網址不要貼到公開群組或社群。展示完就把視窗關掉。

## 卡點儀表板

資料來自兩處，農民照常使用系統就會累積，不必另外輸入：

- 按「我卡住了」選的三個原因
- 上傳公文經白話化後，抽出的駁回／補正理由（缺件、資格不符、逾期…）

寫入當下即去識別化，只留原因、來源、補助類別、鄉鎮、日期，不留身分。

想先看版面（還沒有真實資料時），灌一批示範資料：

```
python scripts\seed_blockers.py
```

⚠️ 示範資料會混進統計，正式上線前刪掉 `data\blockers.jsonl` 重來。

## 補助資料維護後台

在 `.env` 設好密碼後重新啟動，開 http://127.0.0.1:8000/app/admin.html 登入：

```
ADMIN_PASSWORD=你要的密碼
ADMIN_SECRET=隨便一串長亂碼（沒設則每次重啟要重新登入）
```

- **沒設 ADMIN_PASSWORD＝後台不啟用**，不會留下無密碼的後門。
- 申請資格用下拉式條件建構器編輯，欄位只能從 `data\fields.json` 選，打不出未註冊的欄位。
- **存檔前驗證**：與啟動載入共用同一套規則，後台存得進去的資料，重啟一定載得起來。驗證不過就完全不寫檔。
- **存檔即生效**：不必重啟，農民端立刻看到新資料。
- 舊檔自動備份到 `data\backups\`，每次異動記進 `data\admin_audit.jsonl`（承辦不一定會用 Git，稽核不能只靠版本庫）。
- 超過半年沒覆核的補助，清單上會標成橘色「待覆核」。

⚠️ 目前是單一密碼、單人維護的設計。要對外開放或多人各自帳號，需要再加使用者管理與 HTTPS。

## 接上 LINE（讓手機真的能用）

LINE 需要一個公開的 HTTPS 網址把訊息轉給你的電腦：

1. 下載 ngrok（ngrok.com，免費註冊）。
2. 另開一個命令提示字元執行：`ngrok http 8000`
3. 複製畫面上的 `https://xxxx.ngrok-free.app` 網址。
4. 到 LINE Developers → 你的 channel → Messaging API 頁籤：
   - Webhook URL 填 `https://xxxx.ngrok-free.app/line/webhook`
   - 打開「Use webhook」
   - 關閉「自動回應訊息」（不然官方罐頭訊息會跟系統搶話）
5. 用手機加你的官方帳號好友，傳「我要問補助」試試。

⚠️ ngrok 免費版每次重開網址會變，要回去 LINE 後台更新。正式部署時換成固定網址（雲端主機或國網中心環境）。

## 常見問題

- `uvicorn 不是內部或外部命令` → 改用 `python -m uvicorn aidstation.api:app --port 8000`
- 金鑰檢查顯示 ✗ Authentication → 到 console.anthropic.com 確認金鑰沒被撤銷、帳戶有額度
- LINE 沒回應 → 看 ngrok 視窗有沒有進來的請求；沒有就是 Webhook URL 沒設對
